tas_autonomous_control_node

This node subscribes to avc_vel topic, which is the servo velocity computed by the adaptive velocity controller. In autonomous mode, the servo velocity calculated by the adaptive velocity controller is published by tas_autonomous_control_node in servo command message.
